#include <stdio.h>

/**
* main - Entry point
*
* Return: Always 0 (Success)
*/

int main(void)
{
	int x, y;

	for (x = 48; x <= 57; x++)
	{
		for (y = 48; y <= 57; y++)
		{
			if (x == y || y <= x)
			{
			}
			else
			{
				_putchar(x)
					_putchar(y)
					if (x == 56 && y == 57)
					{
					}
					else
					{
						_putchar(44)
							_putchar(' ');
					}
			}
		}
	}
	_putchar('\n');
	return (0);
}
